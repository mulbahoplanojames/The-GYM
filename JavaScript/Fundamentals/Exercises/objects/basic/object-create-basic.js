/*
Create an object called person with properties: name, age, and country. Print the values using dot notation.
*/

const Person = {
  name: "James",
  age: 21,
  country: "Liberia",
};

console.log(Person.name);
console.log(Person.age);
console.log(Person.country);

//Note: == Working with srtings ===

// Using double quotes

const userName = "James";
const state = "Kigali";
console.log(`${userName} is living in ${state}`);

// Using single quotes
const motherNanme = "Mrs. Peterson";
console.log(motherNanme);

// using backticks
const company = `AMOA`;
console.log(company);
